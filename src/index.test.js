var expect = require('chai').expect;
var starWars = require('./index');


describe('starwars-names-cavm', function() {
    it('should work!', function() {
        expect(true).to.be.true;
    });
});
